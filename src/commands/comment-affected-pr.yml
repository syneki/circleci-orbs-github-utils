description: >
  Creates a comment on affected Pull Requests.

parameters:
  commit_sha:
    type: env_var_name
    default: "CIRCLE_SHA1"
    description: The environment variable containing the Commit SHA to get the affected Pull Requests.
steps:
  - run:
      name: Create a comment on PR << parameters.pull_request >>
      command: << include(scripts/release.sh) >>
